---
import BaseLayout from "./Layout.astro";
const { frontmatter } = Astro.props;
---

<BaseLayout pageTitle={frontmatter.title}>
    <div class="gif">
        <img width="300" height="259" alt="RaccoonNotesGif" src="/blog.webp" />
    </div>
    <h2 style="text-decoration:none;">{frontmatter.title}</h2>
    <ul class="badge__list">
        <li>
            <span class="badge badge--item">Auteur</span>{frontmatter.author}
        </li>
        <li>
            <span class="badge badge--item">Publié le</span>{frontmatter.date}
        </li>
        <li>
            <span class="badge badge--item">Catégorie</span>{frontmatter.tag}
        </li>
    </ul>
    <slot />
</BaseLayout>
